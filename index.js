/*const orders = [
  { orderId: 1, customerName: "Alice", amount: 200, status: "completed" },
  { orderId: 2, customerName: "Bob", amount: 450, status: "pending" },
  { orderId: 3, customerName: "Charlie", amount: 120, status: "completed" },
  { orderId: 4, customerName: "David", amount: 500, status: "completed" },
  { orderId: 5, customerName: "Eve", amount: 300, status: "canceled" },
];

const completed = orders.filter((order) => order.status === 'completed');

const amount = completed
    .map((order) => {
        return order.amount;
    })
const total = amount.reduce((sum, amount) => {
    sum = sum + amount;
    return sum;
}, 0);

const sorted_data= completed.sort((a, b) => b.amount - a.amount)
console.log({ total, sorted_data });*/

class Inventory {

    constructor() {

    }

}